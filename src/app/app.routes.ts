import { Routes } from '@angular/router';
import { ElementsListComponent } from './shared/components/elements-list/elements-list.component';

export const routes: Routes = [{ path: '', component: ElementsListComponent }];
