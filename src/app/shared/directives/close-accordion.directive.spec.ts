import { CloseAccordionDirective } from './close-accordion.directive';

describe('CloseAccordionDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseAccordionDirective();
    expect(directive).toBeTruthy();
  });
});
